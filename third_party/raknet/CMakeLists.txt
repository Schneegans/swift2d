set(CMAKE_CXX_FLAGS_RELEASE "/Ox /MP /MD /w /EHsc")
set(CMAKE_CXX_FLAGS_DEBUG "/Ox /MP /MD /w /EHsc")

INCLUDE_DIRECTORIES(
  src
  ../steam
)

FILE(GLOB_RECURSE RAKNET_SRC
  src/*.cpp
)

ADD_LIBRARY(raknet STATIC ${RAKNET_SRC})
