set(CMAKE_CXX_FLAGS_RELEASE "-s -w -O4 -fPIC")
set(CMAKE_CXX_FLAGS_DEBUG "-g -Wall -fPIC")

INCLUDE_DIRECTORIES(
  src
  ../steam
)

FILE(GLOB_RECURSE RAKNET_SRC
  src/*.cpp
)

ADD_LIBRARY(raknet STATIC ${RAKNET_SRC})
